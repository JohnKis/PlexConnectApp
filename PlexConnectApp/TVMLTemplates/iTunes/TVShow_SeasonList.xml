<document>
	<productTemplate>
		<background>
		</background>
		<banner>
			<infoList>
				<info>
					<header>
						<title>Studio</title>
					</header>
					<text>{{ studio }}</text>
				</info>
				<info>
					<header>
						<title>Cast</title>
					</header>
					{{# cast }}
					<text>{{ tag }}</text>
					{{/ cast  }}
				</info>
			</infoList>
			<stack>
				<title>{{ title }}{{^watched}} <badge src="{{ _.imagePath }}/dot.png" class="badge" style="tv-tint-color: rgb(0,122,255);" />{{/watched}}</title>
				<row>
					<!--					<text><badge src="resource://tomato-fresh"/> 99%</text>-->
					<!--					<text>1hr 54min</text>-->
					<text>{{ year }}</text>
					<text>{{ genre }}</text>
					<badge src="resource://{{ contentRating }}" class="badge" style="tv-tint-color: rgb(255,255,255)" />
					<!--					<badge src="resource://cc" class="badge" />-->
				</row>
				<description allowsZooming="true" moreLabel="more" onSelect="Presenter.loadContextWithData('TVShow_SeasonListDescription', '{{ HTMLEscape(title) }}', '{{ HTMLEscape(summary) }}')">{{ summary }}</description>
				<row>
					<buttonLockup>
						<badge src="resource://button-play" />
						<title>Play</title>
					</buttonLockup>
					<buttonLockup>
						<badge src="resource://button-checkmark" />
						<title>Mark as watched</title>
					</buttonLockup>
				</row>
			</stack>
			<heroImg src="{{ thumb }}" />
		</banner>
		<shelf>
			<header>
				<title>Seasons</title>
			</header>
			<section>
				{{# seasons }}
				<lockup onSelect="Presenter.load('TVShow_EpisodeList', '{{ _.pmsId }}', '{{ key }}')">
					<img src="{{ thumb }}" width="226" height="226" />
					{{^ watched}}
					<overlay style="tv-align: right;">
						<badge src="{{ _.imagePath }}/showRibbons/{{ unwatchedCount }}.png" style="tv-position: top-right" width="86" height="86" />
					</overlay>
					{{/ watched}}
					<title>{{ title }}</title>
				</lockup>
				{{/ seasons }}
			</section>
		</shelf>
	</productTemplate>
</document>
